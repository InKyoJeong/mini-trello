import React, { useState } from "react";
import { useForm } from "react-hook-form";

function Todo() {
  return (
    <div>
      <form onSubmit={}>
        <input placeholder="Write a todo" />
      </form>
    </div>
  );
}

export default Todo;
